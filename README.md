# PyNetwork

The following was written in Python 2.6.

List of contents:

ACL - Matches IP to a subnet in a dictionary which is then matched to an ACL name to print the ACL config

DR DIFF - Compares the contents of two files and prints the difference

Netscaler - A collection of scripts utilizing the netscaler Nitro API

SSH -  Contains a paramiko shh script that can be used to deploy/review config
